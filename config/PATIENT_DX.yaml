elf_version: "1.0.0"
subject_id_col: patient_id

# NOTE: CLIF does not currently have a patient_diagnosis table.
# This config defines the expected format for datasets that do.
# patient_diagnosis:
#   diagnosis:
#     code:
#       - PATIENT_DX
#       - ICD
#       - col(icd_version)
#       - col(icd_code)
#     time: col(recorded_dttm)
#     time_format: "%Y-%m-%d %H:%M:%S"
#     text_value: dx_category
#     hospitalization_id: hospitalization_id
